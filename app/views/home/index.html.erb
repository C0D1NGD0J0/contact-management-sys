<div class="landing">
  <div class="dark-overlay landing-inner">
    <div class="container">
      <div class="row">
        <div class="col-sm-12 text-center landing-info">
          <h1 class="header">Contact Manager</h1>
          <p class="lead">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusantium, fuga.</p>
          <p>
            <% if !user_signed_in? %>
              <%= link_to("Sign Up", new_user_registration_path, class: 'btn btn-primary') %>
              <%= link_to("Login", new_user_session_path, class: 'btn btn-warning') %>
            <% end %>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>